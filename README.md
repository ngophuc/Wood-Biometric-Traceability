# README.md

## Environment

`Python 3.10.20`

`torch==2.5.1` with `cu11`

`torchvision==0.20.1`

`torchmetrics==1.9.0` 

## Instructions

Each file is named explicitly and running without having any CLI parameters uses default arguments. `retrieve.py` and `localize.py` need to be run without running `virtual_saw.py` to preserve the virtual cut used by the authors. 